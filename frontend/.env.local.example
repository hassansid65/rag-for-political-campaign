# Copy to frontend/.env.local
#
# The frontend talks to the backend for everything — retrieval, generation, STT
# and TTS. No Azure or Anthropic keys belong here: putting them in a
# NEXT_PUBLIC_* variable ships them to every browser that loads the page.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
